# Quick Task: Improve .gitignore

**ID:** 260508-002
**Description:** Audit and expand `.gitignore` with missing patterns for runtime files, SSL certificates, IDE directories, and build artifacts.

## Tasks

### Task 1: Add missing gitignore patterns
**Action:** Add entries for `.frontend.pid`, `.qwen/`, `ssl-certs/*.key`/`.crt`/`.pem`, `tests/coverage/`, `__pycache__/`, `*.pyc`, `node_modules/` (root).
**Files:** `.gitignore`
**Verify:** `git check-ignore .frontend.pid .qwen/ tests/coverage/` returns all paths
**Done:** 8 new entries added across appropriate sections

### Task 2: Remove tracked runtime file
**Action:** `git rm --cached .frontend.pid` to stop tracking the PID file without deleting it from disk.
**Files:** `.frontend.pid` (tracking status)
**Verify:** `git ls-files .frontend.pid` returns error (not tracked)
**Done:** `.frontend.pid` no longer tracked by git
