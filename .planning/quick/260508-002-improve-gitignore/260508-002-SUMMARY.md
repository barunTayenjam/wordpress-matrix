---
status: complete
quick_id: 260508-002
date: 2026-05-08
---

# Quick Task 260508-002: Improve .gitignore

## Changes Made

### .gitignore
- Added `.frontend.pid` — runtime PID file for the frontend process
- Added `.qwen/` — Qwen AI assistant IDE directory
- Added `ssl-certs/*.key`, `*.crt`, `*.csr`, `*.pem`, `*.p12` — SSL private keys and certificates (security)
- Added `tests/coverage/` and `*.lcov` — PHPUnit test coverage reports
- Added `__pycache__/`, `*.pyc`, `*.pyo` — Python bytecode cache
- Added `node_modules/` — root-level Node.js dependencies (belt-and-suspenders)

### Removed from tracking
- `.frontend.pid` — was previously tracked, now gitignored (PID changes on every restart)

## Root Cause
Runtime artifacts (`.frontend.pid`), IDE settings (`.qwen/`), SSL private keys, test coverage output, and Python cache were not listed in `.gitignore`, risking accidental commits of ephemeral or sensitive files.

## Verification
- `git check-ignore .frontend.pid` → returns path (ignored)
- `git check-ignore .qwen/` → returns path (ignored)
- `git ls-files .frontend.pid` → error (not tracked)
