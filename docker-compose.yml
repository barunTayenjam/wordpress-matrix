networks:
  wp-network:
    driver: bridge

volumes:
  db_data:
  db_backup:
  redis_data:
  ssl_certs:

services:
  # =============================================================================
  # REVERSE PROXY & SSL
  # =============================================================================
  traefik:
    image: traefik:v3.0
    container_name: wp_traefik
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
      - "8080:8080"  # Dashboard
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./config/traefik:/etc/traefik:ro
      - ssl_certs:/etc/ssl/certs
    environment:
      - TRAEFIK_API_DASHBOARD=true
      - TRAEFIK_API_INSECURE=false
    networks:
      - wp-network
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.dashboard.rule=Host(`traefik.${DOMAIN_SUFFIX}`)"
      - "traefik.http.routers.dashboard.tls=true"
      - "traefik.http.routers.dashboard.service=api@internal"
      - "traefik.http.routers.dashboard.middlewares=auth"
      - "traefik.http.middlewares.auth.basicauth.users=${TRAEFIK_AUTH}"
    healthcheck:
      test: ["CMD", "traefik", "healthcheck"]
      interval: 30s
      timeout: 10s
      retries: 3

  # =============================================================================
  # DATABASE
  # =============================================================================
  db-primary:
    image: mysql:8.0
    container_name: wp_mysql
    restart: unless-stopped
    command: sh -c "chown -R mysql:mysql /var/lib/mysql && /usr/local/bin/docker-entrypoint.sh mysqld"
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: ${WORDPRESS_1_DB_NAME}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
    volumes:
      - db_data:/var/lib/mysql
      - ./config/mysql/primary.cnf:/etc/mysql/conf.d/mysql.cnf:ro
    networks:
      - wp-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p${MYSQL_ROOT_PASSWORD}"]
      interval: 30s
      timeout: 10s
      retries: 5

  # =============================================================================
  # CACHING
  # =============================================================================
  redis:
    image: redis:7-alpine
    container_name: wp_redis
    restart: unless-stopped
    command: redis-server --appendonly yes --maxmemory ${REDIS_MAXMEMORY} --maxmemory-policy ${REDIS_MAXMEMORY_POLICY}
    volumes:
      - redis_data:/data
      - ./config/redis/redis.conf:/usr/local/etc/redis/redis.conf:ro
    networks:
      - wp-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 30s
      timeout: 10s
      retries: 3

  memcached:
    image: memcached:alpine
    container_name: wp_memcached
    restart: unless-stopped
    command: memcached -m 256
    networks:
      - wp-network

  # =============================================================================
  # WORDPRESS INSTANCES
  # =============================================================================
  wordpress1:
    image: wordpress:php${PHP_VERSION}-fpm-alpine
    container_name: wp_site1
    restart: unless-stopped
    depends_on:
      - db-primary
      - redis
      - memcached
    environment:
      WORDPRESS_DB_HOST: db-primary:3306
      WORDPRESS_DB_USER: ${MYSQL_USER}
      WORDPRESS_DB_PASSWORD: ${MYSQL_PASSWORD}
      WORDPRESS_DB_NAME: ${WORDPRESS_1_DB_NAME}
      WORDPRESS_DEBUG: ${WORDPRESS_DEBUG}
      WORDPRESS_DEBUG_DISPLAY: ${WORDPRESS_DEBUG_DISPLAY}
      WORDPRESS_DEBUG_LOG: ${WORDPRESS_DEBUG_LOG}
      WORDPRESS_CONFIG_EXTRA: |
        define('WP_REDIS_HOST', 'redis');
        define('WP_REDIS_PORT', 6379);
        define('WP_CACHE_KEY_SALT', '${WORDPRESS_1_DB_NAME}');
        define('WP_ENVIRONMENT_TYPE', 'development');
        define('AUTOMATIC_UPDATER_DISABLED', true);
        define('WP_AUTO_UPDATE_CORE', false);
        define('DISALLOW_FILE_EDIT', false);
        define('WP_DEBUG_DISPLAY', ${WORDPRESS_DEBUG_DISPLAY});
        define('WP_DEBUG_LOG', true);
        define('SCRIPT_DEBUG', true);
        define('SAVEQUERIES', true);
        define('WP_MEMORY_LIMIT', '512M');
    volumes:
      - ./wordpress1:/var/www/html
      - ./config/php:/usr/local/etc/php/conf.d:ro
      - ./logs/wordpress1:/var/log/wordpress
    networks:
      - wp-network

  # Nginx proxy for wordpress1
  nginx-wp1:
    image: nginx:alpine
    container_name: nginx_wp1
    restart: unless-stopped
    depends_on:
      - wordpress1
    volumes:
      - ./wordpress1:/var/www/html:ro
      - ./config/nginx/wordpress.conf:/etc/nginx/conf.d/default.conf:ro
      - ./logs/nginx:/var/log/nginx
    networks:
      - wp-network
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.wordpress1.rule=Host(`wordpress1.${DOMAIN_SUFFIX}`)"
      - "traefik.http.routers.wordpress1.tls=true"
      - "traefik.http.routers.wordpress1.middlewares=security-headers"
      - "traefik.http.services.wordpress1.loadbalancer.server.port=80"
      - "traefik.http.services.wordpress1.loadbalancer.healthcheck.path=/wp-admin/admin-ajax.php"
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost/wp-admin/admin-ajax.php?action=heartbeat || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3

  wordpress2:
    image: wordpress:php${PHP_VERSION}-fpm-alpine
    container_name: wp_site2
    restart: unless-stopped
    depends_on:
      - db-primary
      - redis
      - memcached
    environment:
      WORDPRESS_DB_HOST: db-primary:3306
      WORDPRESS_DB_USER: ${MYSQL_USER}
      WORDPRESS_DB_PASSWORD: ${MYSQL_PASSWORD}
      WORDPRESS_DB_NAME: ${WORDPRESS_2_DB_NAME}
      WORDPRESS_DEBUG: ${WORDPRESS_DEBUG}
      WORDPRESS_DEBUG_DISPLAY: ${WORDPRESS_DEBUG_DISPLAY}
      WORDPRESS_DEBUG_LOG: ${WORDPRESS_DEBUG_LOG}
      WORDPRESS_CONFIG_EXTRA: |
        define('WP_REDIS_HOST', 'redis');
        define('WP_REDIS_PORT', 6379);
        define('WP_CACHE_KEY_SALT', '${WORDPRESS_2_DB_NAME}');
        define('WP_ENVIRONMENT_TYPE', 'development');
        define('AUTOMATIC_UPDATER_DISABLED', true);
        define('WP_AUTO_UPDATE_CORE', false);
        define('DISALLOW_FILE_EDIT', false);
        define('WP_DEBUG_DISPLAY', ${WORDPRESS_DEBUG_DISPLAY});
        define('WP_DEBUG_LOG', true);
        define('SCRIPT_DEBUG', true);
        define('SAVEQUERIES', true);
        define('WP_MEMORY_LIMIT', '512M');
    volumes:
      - ./wordpress2:/var/www/html
      - ./config/php:/usr/local/etc/php/conf.d:ro
      - ./logs/wordpress2:/var/log/wordpress
    networks:
      - wp-network

  # Nginx proxy for wordpress2
  nginx-wp2:
    image: nginx:alpine
    container_name: nginx_wp2
    restart: unless-stopped
    depends_on:
      - wordpress2
    volumes:
      - ./wordpress2:/var/www/html:ro
      - ./config/nginx/wordpress2.conf:/etc/nginx/conf.d/default.conf:ro
      - ./logs/nginx:/var/log/nginx
    networks:
      - wp-network
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.wordpress2.rule=Host(`wordpress2.${DOMAIN_SUFFIX}`)"
      - "traefik.http.routers.wordpress2.tls=true"
      - "traefik.http.routers.wordpress2.middlewares=security-headers"
      - "traefik.http.services.wordpress2.loadbalancer.server.port=80"
      - "traefik.http.services.wordpress2.loadbalancer.healthcheck.path=/wp-admin/admin-ajax.php"
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost/wp-admin/admin-ajax.php?action=heartbeat || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3

  # =============================================================================
  # DEVELOPMENT TOOLS
  # =============================================================================
  phpmyadmin:
    image: phpmyadmin:latest
    container_name: wp_phpmyadmin
    restart: unless-stopped
    depends_on:
      - db-primary
    environment:
      PMA_HOST: db-primary
      PMA_USER: root
      PMA_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      UPLOAD_LIMIT: 100M
    networks:
      - wp-network
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.phpmyadmin.rule=Host(`phpmyadmin.${DOMAIN_SUFFIX}`)"
      - "traefik.http.routers.phpmyadmin.tls=true"
      - "traefik.http.services.phpmyadmin.loadbalancer.server.port=80"

  mailhog:
    image: mailhog/mailhog:latest
    container_name: wp_mailhog
    restart: unless-stopped
    networks:
      - wp-network
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.mailhog.rule=Host(`mailhog.${DOMAIN_SUFFIX}`)"
      - "traefik.http.routers.mailhog.tls=true"
      - "traefik.http.services.mailhog.loadbalancer.server.port=8025"

  wpcli:
    image: wordpress:cli-2.8
    container_name: wp_cli
    restart: unless-stopped
    depends_on:
      - db-primary
      - wordpress1
      - wordpress2
    volumes:
      - ./wordpress1:/var/www/html/wordpress1
      - ./wordpress2:/var/www/html/wordpress2
    networks:
      - wp-network
    command: tail -f /dev/null

  # =============================================================================
  # FILE SYNC & HOT RELOAD
  # =============================================================================
  file-sync:
    image: node:20-alpine
    container_name: wp_file_sync
    working_dir: /app
    restart: unless-stopped
    volumes:
      - ./docker/file-sync:/app
      - /var/run/docker.sock:/var/run/docker.sock
    networks:
      - wp-network
    environment:
      - CHOKIDAR_USEPOLLING=true
      - SYNC_PATHS=/app/wordpress1,/app/wordpress2
      - RELOAD_CONTAINERS=wp_site1,wp_site2
    command: sh -c "npm install && node index.js"